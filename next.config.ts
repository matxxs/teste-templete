// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // Change 'standalone' to 'export' for GitHub Pages
  /* config options here */
};

export default nextConfig;
